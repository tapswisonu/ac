# Replace Dashboard UI with Floating Service Cards in Hero Section

## Overview
Remove the existing dashboard illustration from the Hero Section and replace it with a set of modern, floating service cards that highlight key services. 

## Changes

### 1. [app/page.tsx](file:///d:/Tapswi/ac/legal-services/app/page.tsx)
- **Imports**: Add `FaFileInvoiceDollar`, `FaTrademark`, `FaBuilding`, `FaUtensils` from `react-icons/fa`. Remove unused variable `FaFire` if no longer used.
- **Remove Variables**: Delete `dashboardStats`, `floatingCards`, and `complianceTasks` arrays which were used by the Dashboard UI.
- **Remove Component**: Delete the [DashboardIllustration](file:///d:/Tapswi/ac/legal-services/app/page.tsx#85-180) function.
- **Add Component**: Create a new `FloatingServiceCards` function.
  - Setup 4 cards for: `GST Registration`, `Trademark Filing`, `Company Registration`, `FSSAI License`.
  - Apply `bg-white`, `rounded-xl`, `shadow-xl`, `px-6 py-4`, `flex items-center gap-3`.
  - Stagger horizontally using translation classes (e.g. `-translate-x-12`, `translate-x-12`).
  - Add `animate-float` animation and background radial gradient glow (orange/yellow with `blur-3xl`).
- **Update Hero Grid**: 
  - Change the right column wrapper from `className="hidden lg:flex ..."` to `className="flex mt-8 lg:mt-0 lg:justify-end items-center..."` so it shows on mobile screens.
  - Render `<FloatingServiceCards />` within this wrapper.
- **Tailwind Config**: We will utilize the existing `animate-float` utility or create one inline.

## Verification
- Load the homepage.
- Observe the right side of the hero section for 4 floating cards with staggered alignment and smooth floating animations up and down.
- Observe on mobile view, the cards appear vertically stacked below the main header text.
